# Capability + Action + MCP Rail

Capability Registry is source of business operations. ExecutionContext + centralized authorization on every call. Side effects use ActionRequest/Attempt + idempotency/reconciliation.

Machine API and MCP are thin adapters. Safe/internal MCP V1 only. Same fixtures must produce semantic parity across service/API/MCP. Future R4/R5 actions disabled.
