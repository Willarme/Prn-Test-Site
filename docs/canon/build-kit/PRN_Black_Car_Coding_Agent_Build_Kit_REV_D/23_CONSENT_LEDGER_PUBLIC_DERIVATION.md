# Consent Ledger + Public-Safe Derivation — Revision D

Private Source -> Derived Local -> Public-Safe Insight. Raw address/narrative/photos/contact/referral identity stay private by default.

Immutable DisclosureVersion + append-only ConsentEvent for material permissions. Channel consent separate. Unknown rights = restricted.

Public-safe derivation never copies raw free text/photos, removes direct identifiers, coarsens location/time as needed, blocks rare/small-cell re-identification, stores lineage + policy version and supports re-evaluation/unpublish.
