## Wave 0 - Canon/contracts
Read all canon + prior gate report. Implement **Wave 0 - Canon/contracts only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 1 - Durable data/rights/provenance
Read all canon + prior gate report. Implement **Wave 1 - Durable data/rights/provenance only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 2 - Domain services/AI/Safety/A01-A02
Read all canon + prior gate report. Implement **Wave 2 - Domain services/AI/Safety/A01-A02 only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 3 - Auth/action/API/MCP
Read all canon + prior gate report. Implement **Wave 3 - Auth/action/API/MCP only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 4 - Customer Lite/Trust
Read all canon + prior gate report. Implement **Wave 4 - Customer Lite/Trust only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 5 - Provider resolver/recommendation
Read all canon + prior gate report. Implement **Wave 5 - Provider resolver/recommendation only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 6 - Page factory
Read all canon + prior gate report. Implement **Wave 6 - Page factory only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 7 - Feature Lab
Read all canon + prior gate report. Implement **Wave 7 - Feature Lab only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 8 - Admin/agents/A36/A25
Read all canon + prior gate report. Implement **Wave 8 - Admin/agents/A36/A25 only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 9 - Hardening
Read all canon + prior gate report. Implement **Wave 9 - Hardening only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 10 - Closed staging pilot
Read all canon + prior gate report. Implement **Wave 10 - Closed staging pilot only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.

## Wave 11 - Production trial
Read all canon + prior gate report. Implement **Wave 11 - Production trial only**. Inspect current code first, list exact files/migrations/tests, and list explicit out-of-scope items. Preserve stable IDs, provenance, rights, event names and capability contracts. Use compatibility-safe migrations. Run all relevant tests and deploy to preview/staging. Update CHANGELOG/release record. Return PASS/FAIL + blockers and STOP.
