export default function Home() {
  // Wave 0: canon + contracts only. No customer-facing features exist yet,
  // and no business logic ever lives in this layer.
  return (
    <main style={{ fontFamily: "system-ui", padding: "4rem 2rem", maxWidth: 640 }}>
      <h1>Property Response Network</h1>
      <p>Build in progress — Wave 0 (canon and contracts). Nothing to see here yet.</p>
    </main>
  );
}
