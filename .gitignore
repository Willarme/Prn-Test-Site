# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — secrets NEVER committed
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# test
/coverage

# local runtime data (fixture journeys) — never committed
/data/runtime/

# the owner's live AI policy document — runtime state, never committed.
# The shipped defaults stay all-off (proven by ai.flags-off-parity.test.ts);
# an owner turns the engine on per environment by writing this file, exactly
# as canon intends ("a document an owner edits live"). Owner directive
# 2026-08-26: ON in the local test environment.
/data/ai-policy.json
